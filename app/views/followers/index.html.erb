<p id="notice"><%= notice %></p>

<h1><%= current_user.full_name %> Follows</h1>

<table width="100%">
  <thead>
    <tr>
      <th>User</th>

      <th colspan="2"></th>
    </tr>
  </thead>
<!-- Called from /Users/ThesMac/.rvm/gems/ruby-2.3.1/gems/activesupport-4.2.0/lib/active_support/dependencies.rb:240:in `load_dependency'
  Follower Load (0.9ms)  SELECT  "followers".* FROM "followers"  ORDER BY "followers"."id" ASC LIMIT 1
  Follower Load (0.9ms)  SELECT  "followers".* FROM "followers"  ORDER BY "followers"."id" ASC LIMIT 1
 => #<Follower id: 14, user_id: 4, follower_id: 1, created_at: "2017-10-08 20:29:33", updated_at: "2017-10-08 20:29:33"> 
2.3.1 :002 > exit -->
  <tbody>
    <% @followed.each do |follower| %>
      <tr>
        <td><%= link_to follower.full_name, follower %>
        <% @user_boards = User.find(follower.id).boards %>
	<ul class="follow">
		<% @user_boards.each do |b| %>
			<li><%= link_to b.name, board_path(b) %>, <%= b.pinnings.count %> pins</li>
		<% end %>
	</ul></td>

        <td><%= link_to 'Unfollow', follower, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Follower', new_follower_path %>
